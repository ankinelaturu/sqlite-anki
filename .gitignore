# Rust
/target/
**/*.rs.bk

# Node
node_modules/
dist/
*.tsbuildinfo

# Models (large binaries — add pinned artifacts explicitly when ready)
models/**/*.onnx
models/**/*.onnx.data

# WASM build output
packages/wasm/dist/

# Vendored SQLite + Emscripten (fetched by scripts/build-wasm.sh)
vendor/sqlite/
vendor/emsdk/

# IDE / OS
.idea/
.vscode/
.DS_Store
*.log

# Env
.env
.env.*

# Claude Code — personal/machine-local settings + throwaway worktrees
.claude/settings.local.json
.claude/worktrees/

