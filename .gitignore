# Rust
/target/
**/*.rs.bk

# Node
node_modules/
dist/
*.tsbuildinfo

# Models (large binaries — add pinned artifacts explicitly when ready)
models/**/*.onnx
models/**/*.onnx.data

# WASM build output
packages/wasm/dist/

# Vendored SQLite + Emscripten (fetched by scripts/build-wasm.sh)
vendor/sqlite/
vendor/emsdk/

# IDE / OS
.idea/
.vscode/
.DS_Store
*.log

# Env
.env
.env.*

# explorer: synced wasm artifacts (copied from packages/wasm/dist by sync-wasm)
apps/explorer/public/sqlite3-bundler-friendly.mjs
apps/explorer/public/sqlite3.wasm
apps/explorer/public/sqlite3-opfs-async-proxy.js
